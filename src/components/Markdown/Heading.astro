---
import MaybeLink from "@components/MaybeLink.astro";

export interface Props {
    level: number;
    id?: string;
}
console.log(Astro.props)
const { level, id } = Astro.props;
const Tag = `h${level}`
---
<MaybeLink href={id ? `#${id}` : undefined}>
    <Tag><slot/></Tag>
</MaybeLink>
